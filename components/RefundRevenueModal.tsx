// This component is deprecated. Its functionality for issuing refunds has
// been removed from the platform to align with the "no billing, no purchase" policy.
// The financial ledger is now treated as an immutable record of revenue.

import React from 'react';

const RefundRevenueModal: React.FC = () => {
  return null;
};

export default RefundRevenueModal;
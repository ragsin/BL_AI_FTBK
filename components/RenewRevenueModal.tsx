// This component is deprecated. Its functionality for renewing enrollments has
// been removed from the platform to align with the "no billing, no purchase" policy.
// Revenue entries are now added for new enrollments only via the RevenueFormModal.

import React from 'react';

const RenewRevenueModal: React.FC = () => {
  return null;
};

export default RenewRevenueModal;